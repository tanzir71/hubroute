{
  "name": "hubroute-vercel",
  "version": "0.1.0",
  "private": true,
  "description": "HubRoute Vercel-ready production starter with hosted SQLite-compatible storage",
  "type": "module",
  "scripts": {
    "seed": "node scripts/seed.mjs",
    "deploy": "vercel deploy --prod",
    "dev": "vercel dev"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4"
  },
  "engines": {
    "node": ">=20"
  }
}
