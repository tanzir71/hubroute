{
  "name": "hubroute",
  "version": "0.1.0",
  "private": true,
  "description": "HubRoute PHP and SQLite parcel operations app",
  "scripts": {
    "build": "npm run sync:demo",
    "build:demo": "node scripts/build-demo.mjs",
    "sync:demo": "node scripts/build-demo.mjs && node scripts/sync-demo.mjs",
    "check:demo": "node scripts/build-demo.mjs --check && node scripts/sync-demo.mjs --check",
    "smoke:demo": "node scripts/smoke-demo.mjs",
    "package:php": "node scripts/package-php.mjs",
    "deploy:shared": "npm run package:php",
    "maintainer:deploy:demo": "npm run sync:demo && npm test && npx --yes vercel deploy --prod",
    "maintainer:deploy:demo:preview": "npm run sync:demo && npm test && npx --yes vercel deploy",
    "test:domain": "node --test tests/*.test.mjs",
    "test": "npm run test:domain && npm run check:demo && npm run smoke:demo"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0"
  }
}
